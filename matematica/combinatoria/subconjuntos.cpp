// ============================================================================
// Gerar subconjuntos
// O(2^N)
// ============================================================================

/*

Dado um conjunto A (em forma de bitmask),
o algoritmo gera todos os subconjuntos de A.

*/

void process_subsets(const int a) {
    int b = 0;
    do {
        // Processar subconjunto
    } while (b = (b - a) & a);
}
